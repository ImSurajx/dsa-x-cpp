#include<iostream>
using namespace std;
int main(){
    int number = 0;
    if(number>0) cout<< "Number is positive."<<endl; // false no output.
    if(number => 0) cout <<  "Number is not negative."<<endl; // error b/c of the greater than equal to b/c it's written wrong it must be >=.
}