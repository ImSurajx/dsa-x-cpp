#include<iostream>
using namespace std;
int main(){
    int j;
    // garbage values;
    // when we doesn't initalize the variable computer store garbage value by defualt.
    while(j<=10){
        cout<<j<<endl;
        j = j+1;
    } // nothing will happend / error
    return 0;
}