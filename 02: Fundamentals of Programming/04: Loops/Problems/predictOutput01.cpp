#include <iostream>
using namespace std;
int main()
{
    while ('a' < 'b')
        cout << "malayalam is a palindrome." << endl; // infinite loop.
    return 0;
}
/*
infinite loop: An infinite loop is a loop that never ends — it keeps running forever unless the program is forcibly stopped or the loop is broken using something like break.
*/