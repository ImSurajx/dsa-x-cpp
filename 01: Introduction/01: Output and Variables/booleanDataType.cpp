#include<iostream>
using namespace std;
int main(){
    // boolean always store true & flase if it's true it's numeric value is 1 & if it's false then it's 0;
    // if we give any input in bool data type except 0 it will be true or 1
    // if we print expresion like (7>4) if the expersion condition is true it's return 1(true) else 0(false).
    bool flag = false; // true / false
    cout<<flag;
    return 0;
}