// sum of two given numbers
#include <iostream>
using namespace std;
int main()
{
    int x, y;
    cout << "enter first number : ";
    cin >> x; // user will gave value of x;
    cout << "enter second number : ";
    cin >> y; // user will gave value of y;
    cout << "the sum of the gaven numbers is : " << x + y << endl;
    return 0;
}