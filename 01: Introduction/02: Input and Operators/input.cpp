// taking input
#include <iostream>
using namespace std;
int main()
{
    float radius;
    cout << "enter radius of cricel : ";
    cin >> radius; // take input inside variable.
    cout << "the area of circle is : " << 3.1415 * (radius * radius) << endl;
    return 0;
}